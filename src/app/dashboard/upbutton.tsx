import { ChevronUp } from "lucide-react"

export const UpButton = () => {
  return (
    <ChevronUp 
      className="border-2 rounded-full py-1 fixed bottom-5 right-5 cursor-pointer" 
      size={30} 
    />
  )
}